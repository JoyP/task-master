extends ../shared/template
block content
  h2 Priorities

  table
    thead
      tr
        th Name
        th Color
        th value
    tbody
      each priority in priorities
        tr
          td= priority.name
          td(style='background-color:#{priority.color};')
          td= priority.value

